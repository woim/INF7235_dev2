#include "resampling.h"
#include "mpi.h"

void resampling_mpi1(
    double matrix[2][3],
    Image* source,
    Image* destination);
void resampling_mpi1(
    double matrix[2][3],
    Image* source,
    Image* destination);
